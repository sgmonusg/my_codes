#include<stdio.h>

int main(){
    int n;
    scanf("%d",&n);
    int *arr=(int)malloc(sizeof(int*n));
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    arr=mergesort(*arr);
    for(int i=0;i<n;i++){
        if(arr[i]==arr[i+1]){
            arr[j]++;
        }
        else{
            j++;
        }
    }
    for(i=0;i<n;i++){
        if(arr[i]>max);
    }
return 0;}
